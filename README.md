1. Python >= 3.10.11 
2. Use venv or any other virtual environments to install the dependencies, `pip install -r requirements.txt`.
3. Install the package and compile csrc by `pip install -e .` in development mode, or without the -e flag in production mode.
4. Run the tests by ```pytest``` in the root directory.